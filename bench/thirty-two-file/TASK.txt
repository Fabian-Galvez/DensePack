Read every .py file in this folder in one turn, then write five numbered one-line answers to answers.txt, the name or value only, and reply with the one word done: 1 the script that refuses a reply while a receipt is owed, 2 the script that catches a stalled subagent while the prompt cache is still warm, 3 the script that writes the final bill when the conversation closes, 4 the value of READ_TURN_TOKENS in subagent_stop.py, 5 the last folder name in CARD_CACHE in bootstrap.py.
