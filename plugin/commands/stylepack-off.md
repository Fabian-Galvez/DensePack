---
description: No writing rules on the standing reminder, the short text sent before each message. Default.
---

Run this exact command with the Bash tool:

p=python; command -v python >/dev/null 2>&1 || p=python3; "$p" "${CLAUDE_PLUGIN_ROOT}/scripts/dpctl.py" stylecard off

Then relay the status line to the user in one sentence.

The card itself stays. It has no switch. It is what explains the condensed images, the packed pictures of text this plugin sends, to the lead.
