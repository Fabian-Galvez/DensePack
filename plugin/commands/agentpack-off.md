---
description: Delegation rules off. Packing and receipts keep working.
---

Run this exact command with the Bash tool:

p=python; command -v python >/dev/null 2>&1 || p=python3; "$p" "${CLAUDE_PLUGIN_ROOT}/scripts/dpctl.py" agentpack off

Then relay the status line to the user in one sentence.

/densepack puts them back to force, the default.
