---
description: Draw every image at 10 px, for an Opus 5 lead.
---

Run this exact command with the Bash tool:

p=python; command -v python >/dev/null 2>&1 || p=python3; "$p" "${CLAUDE_PLUGIN_ROOT}/scripts/dpctl.py" reader opus

Then relay the status line to the user in one sentence.

Opus 5 scored 1 of 10 at 8 px and 10 of 10 at 10 px, measured 18 August 2026. The bigger type is not a preference. Bigger type means a bigger image. The saving is 56.5 percent rather than Fable's 73.5.

This does NOT stop Fable and Opus agents exchanging images. That is on always. What it does is set the size the LEAD reads at.
