---
description: Draw every image at 8 px, for a Fable 5 lead. An override; the plugin reads the lead's model itself.
---

Run this exact command with the Bash tool:

p=python; command -v python >/dev/null 2>&1 || p=python3; "$p" "${CLAUDE_PLUGIN_ROOT}/scripts/dpctl.py" reader fable

Then relay the status line to the user in one sentence.

8 px is the smallest size two cold Fable 5 readers read with every answer exact, measured 14 August 2026. It saves 73.5 percent over 261 real reports. Use this to come back after /opuspack.
